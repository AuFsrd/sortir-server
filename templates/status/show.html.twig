{% extends 'base.html.twig' %}

{% block title %}Status{% endblock %}

{% block body %}
    <h1>Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_status_index') }}">Status list</a>

    <a href="{{ path('app_status_edit', {'id': status.id}) }}">Edit</a>

    {{ include('status/_delete_form.html.twig') }}
{% endblock %}
