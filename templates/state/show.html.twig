{% extends 'base.html.twig' %}

{% block title %}State{% endblock %}

{% block body %}
    <h1>State</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ state.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_state_index') }}">State list</a>

    <a href="{{ path('app_state_edit', {'id': state.id}) }}">Edit</a>

    {{ include('state/_delete_form.html.twig') }}
{% endblock %}
